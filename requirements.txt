# Core -- everything in src/ runs with these alone.
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
pyyaml>=6.0

# Optional accelerators. The code degrades gracefully if any is absent:
#   xgboost/lightgbm -> falls back to sklearn HistGradientBoostingRegressor
#   torch            -> LSTM path disabled, baselines still run
#   pyarrow          -> Parquet cache falls back to pickle
xgboost>=2.0
lightgbm>=4.0
torch>=2.0
pyarrow>=14.0

# Development
pytest>=7.4
pytest-cov>=4.1
ruff>=0.1
